---
name: alpha
description: Lint fixture; shares one-off flags with siblings.
metadata:
  version: "0.1.0"
---

# alpha

### Arguments
- `--input`, `-i` — input Zarr.
- `--output`, `-o` — output Zarr.
- `--method` — reducer.
- `--window` — window width.
