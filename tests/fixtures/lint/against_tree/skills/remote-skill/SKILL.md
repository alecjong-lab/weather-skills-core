---
name: remote-skill
description: Lint fixture; an --against corpus skill.
metadata:
  version: "0.1.0"
---

# remote-skill

### Arguments
- `--input`, `-i` — input Zarr.
- `--output`, `-o` — output Zarr.
- `--method` — percentile.
