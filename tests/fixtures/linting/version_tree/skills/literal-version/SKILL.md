---
name: literal-version
description: Lint fixture; the decorator receives a literal version, not _SKILL_VERSION.
metadata:
  version: "0.1.0"
---

# literal-version

### Arguments
- `--input`, `-i` — input Zarr.
- `--output`, `-o` — output Zarr.
