---
name: beta
description: Lint fixture; shares --method with alpha at the same shape.
metadata:
  version: "0.1.0"
---

# beta

### Arguments
- `--input`, `-i` — input Zarr.
- `--output`, `-o` — output Zarr.
- `--method` — reducer.
