---
name: gamma
description: Lint fixture; shares flag names with alpha and beta at divergent shapes.
metadata:
  version: "0.1.0"
---

# gamma

### Arguments
- `--input`, `-i` — input Zarr.
- `--output`, `-o` — output Zarr.
- `--method` — aggregation method with divergent choices.
- `--window` — window width in degrees.
